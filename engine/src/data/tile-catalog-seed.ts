import type { TileDef } from '../types/tile-types.js';

export const TILE_CATALOG_SEED: readonly TileDef[] = [{ id: '9', color: 'yellow', quantity: -1, cities: [], paths: [{ from: { kind: 'edge', edge: 0 }, to: { kind: 'edge', edge: 3 } }] }];
